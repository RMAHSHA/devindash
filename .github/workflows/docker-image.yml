name: DevinMonitor

on:
  push:
    branches: [ "devin/1775377498-initial-commit" ]
  pull_request:
    branches: [ "devin/1775377498-initial-commit" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
